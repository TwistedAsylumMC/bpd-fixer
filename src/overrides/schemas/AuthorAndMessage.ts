import type { Override } from '../types.js';

const override: Override = {
  reason: "Both the author name and the message body are always written for this variant.",
  required: {
    "Player Name": true,
    "Message": true,
  },
};

export default override;
