import type { Override } from '../types.js';

const override: Override = {
  reason: "The subcommand value list is always written, empty if there are none.",
  required: {
    "SubCommand values": true,
  },
};

export default override;
