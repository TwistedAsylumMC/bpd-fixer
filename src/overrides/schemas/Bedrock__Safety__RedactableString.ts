import type { Override } from '../types.js';

const override: Override = {
  reason: "Both strings are always written; the redacted form is not optional.",
  required: {
    "Redacted": true,
  },
};

export default override;
