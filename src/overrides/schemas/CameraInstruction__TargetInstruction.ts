import type { Override } from '../types.js';

const override: Override = {
  reason: "Target Center Offset is an optional; only Target Actor ID is unconditional.",
  required: {
    "Target Center Offset": false,
  },
};

export default override;
