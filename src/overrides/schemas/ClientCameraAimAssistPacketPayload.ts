import type { Override } from '../types.js';

const override: Override = {
  reason: "Allow aim assist is always written, so all three fields are unconditional.",
  required: {
    "Allow aim assist": true,
  },
};

export default override;
