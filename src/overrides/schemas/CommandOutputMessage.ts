import type { Override } from '../types.js';

const override: Override = {
  reason: "The parameter list is always written, empty if the message takes no parameters.",
  required: {
    "Parameters": true,
  },
};

export default override;
