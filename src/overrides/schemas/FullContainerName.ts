import type { Override } from '../types.js';

const override: Override = {
  reason: 'Dynamic ID is an optional value (present only for dynamic containers).',
  required: {
    'Dynamic ID': false,
  },
};

export default override;
