import type { Override } from '../types.js';

const override: Override = {
  reason: "Entries is always written; the schema declares no required fields at all.",
  required: {
    "Entries": true,
  },
};

export default override;
