import type { Override } from '../types.js';

const override: Override = {
  reason: "Molang Variables is an optional (bool + string), not an unconditional field.",
  required: {
    "Molang Variables": false,
  },
};

export default override;
